- job:
    name: test-kuber
    project-type: pipeline
    description: |
        Do not edit this job through the web!
    concurrent: false
    properties:
    - build-discarder:
        days-to-keep: -1
        num-to-keep: 10
        artifact-days-to-keep: -1
        artifact-num-to-keep: 10
    pipeline-scm:
      scm:
        - git:
            url: https://github.com/hors/jenkins-pipelines.git
            branches:
            - test_cloud_kubernetes_job
            wipe-workspace: false
      lightweight-checkout: true
      script-path: cloud/jenkins/test_kuber_tests_job.groovy
    parameters:
    - string:
        name: GIT_REPO
        default: https://github.com/percona/percona-xtradb-cluster-operator
        description: percona/percona-xtradb-cluster-operator repository
    - string:
        name: GIT_BRANCH
        default: master
        description: Tag/Branch for percona/percona-xtradb-cluster-operator repository
